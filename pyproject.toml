[tool.poetry]
name = "bouncer-config"
version = "0.1.0"
description = ""
authors = ["Chien-Ku Chen <cchen@netskope.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
kubernetes = "^29.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

